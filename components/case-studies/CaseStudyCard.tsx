import Image from 'next/image';
import Link from 'next/link';
import { ArrowUpRight } from 'lucide-react';
import type { CaseStudy } from '@/lib/case-studies';

export default function CaseStudyCard({ study, index }: { study: CaseStudy; index: number }) {
  return <Link href={`/case-studies/${study.slug}`} className="group block border-b border-slate-200 px-6 py-16 transition-colors hover:bg-white lg:px-8 lg:py-24"><article className="mx-auto grid max-w-7xl items-center gap-10 lg:grid-cols-12 lg:gap-14"><div className="lg:col-span-2"><p className="text-xs font-bold uppercase tracking-[0.2em] text-slate-400">{String(index + 1).padStart(2, '0')}</p><p className="mt-3 font-montserrat text-lg font-bold text-slate-900">{study.client}</p><p className="mt-1 text-sm text-slate-500">{study.industry}</p></div><div className="relative aspect-[4/3] overflow-hidden rounded-[2rem] bg-slate-200 lg:col-span-5"><Image src={study.thumbnail} alt="" fill sizes="(max-width: 1024px) 100vw, 42vw" className="object-cover transition-transform duration-700 group-hover:scale-105" /></div><div className="lg:col-span-5"><h2 className="font-montserrat text-4xl font-bold tracking-tight text-slate-950 transition-colors group-hover:text-blue-600 md:text-5xl">{study.title}</h2><p className="mt-5 max-w-xl font-poppins leading-relaxed text-slate-500">{study.summary}</p><div className="mt-7 flex flex-wrap gap-6 border-l-2 border-blue-600 pl-4">{study.metrics.map((metric) => <div key={metric.label}><strong className="block font-montserrat text-2xl text-slate-950">{metric.value}</strong><span className="text-[10px] font-bold uppercase tracking-widest text-slate-400">{metric.label}</span></div>)}</div><span className="mt-8 inline-flex items-center gap-2 text-xs font-bold uppercase tracking-widest text-blue-600">Read case study <ArrowUpRight size={16} /></span></div></article></Link>;
}
